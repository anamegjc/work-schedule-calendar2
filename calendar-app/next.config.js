/** @type {import('next').NextConfig} */
const nextConfig = {
    output: 'export',
    basePath: '/work-schedule-calendar',
    images: {
      unoptimized: true
    }
  }
  
  module.exports = nextConfig
